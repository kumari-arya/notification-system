# This file is a TEMPLATE only — do not fill in real values and commit
# this to git. Secrets belong in git the same way passwords belong in
# plaintext emails: never.
#
# Create the real secret imperatively instead:
#   kubectl create secret generic app-secrets \
#     --from-literal=DATABASE_URL='postgres://notifuser:devpass@postgres:5432/notifications'
